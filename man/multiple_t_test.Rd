% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/t_tests.R
\name{multiple_t_test}
\alias{multiple_t_test}
\title{Multiple T-Tests Against One Classifier}
\usage{
multiple_t_test(
  .data,
  .var_list = names(dplyr::select(.data, dplyr::where(is.numeric))),
  .class,
  alternative = "two.sided",
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{.var_list}{Character vector of column names to test (default: every
numeric column in \code{.data}).}

\item{.class}{Name (string) of a binary classification column.}

\item{alternative}{One of \code{"two.sided"} (default), \code{"greater"}, or \code{"less"}.}

\item{conf.level}{Confidence level for the interval.}

\item{...}{Additional arguments passed to \code{\link{single_t_test}} (and
on to \code{\link[stats]{t.test}}) — e.g. \code{paired} and \code{.id}.}
}
\value{
A tibble with one row per tested variable.
}
\description{
Runs \code{\link{single_t_test}} for every (or a chosen set of) numeric
column against a single binary classifier.
}
\examples{
mtcars |>
  dplyr::mutate(am = factor(am)) |>
  multiple_t_test(.class = "am")
}
