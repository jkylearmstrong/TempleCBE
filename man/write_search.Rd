% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_wrappers.R
\name{write_search}
\alias{write_search}
\title{Search a Directory Tree for File-Write Calls}
\usage{
write_search(path, include_comments = FALSE, workers = NULL)
}
\arguments{
\item{path}{Character. Root directory to search.}

\item{include_comments}{Logical. Passed through to \code{\link{find_code}}.
Defaults to \code{FALSE}.}

\item{workers}{Integer number of parallel workers to use when
\pkg{future}/\pkg{furrr} are installed. Defaults to \code{NULL}, which
caps at available cores minus one. Falls back to sequential search when
\pkg{future}/\pkg{furrr} are not installed.}
}
\value{
A tibble of matches (see \code{\link{find_code}}) with an
  additional \code{pattern} column recording which write function matched.
}
\description{
Convenience wrapper around \code{\link{find_code}} that searches for common
data-export function calls (\code{write_xlsx}, \code{saveRDS},
\code{write.csv}, etc.) across a directory tree.
}
\examples{
\dontrun{
write_search(here::here("analysis"))
}
}
