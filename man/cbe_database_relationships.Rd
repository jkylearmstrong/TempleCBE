% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbe_database_relationships.R
\name{cbe_database_relationships}
\alias{cbe_database_relationships}
\alias{as_tbl_graph.cbe_database_relationships}
\alias{cbe_find_shared_keys}
\alias{cbe_check_key_integrity}
\title{Analyze Relationships and Linkages Across Datasets in an R Database}
\usage{
cbe_database_relationships(db, id_cols = NULL)

\method{as_tbl_graph}{cbe_database_relationships}(x, ...)

cbe_find_shared_keys(db)

cbe_check_key_integrity(
  db,
  id_col,
  master_dataset = NULL,
  compare = FALSE,
  tolerance = 1e-07,
  ...
)
}
\arguments{
\item{db}{A named list of data frames (an R database).}

\item{id_cols}{Optional character vector of key/ID column names to evaluate. If \code{NULL},
shared keys are automatically detected across datasets.}

\item{x}{A \code{cbe_database_relationships} object.}

\item{...}{Additional arguments passed to \code{\link{cbe_compare_df}}.}

\item{id_col}{Character string specifying the identifier column to check.}

\item{master_dataset}{Character string naming the primary cohort table. If provided,
checks which IDs in other tables are absent from the master cohort table.}

\item{compare}{Logical; if \code{TRUE} and \code{master_dataset} is supplied,
runs \code{\link{cbe_compare_df}} on overlapping variables between the master dataset
and child datasets, attaching the comparison results.}

\item{tolerance}{Numeric comparison tolerance passed to \code{\link{cbe_compare_df}}.}
}
\value{
A tibble summarizing dataset pairs, shared key columns, record counts,
  overlap counts, coverage percentages, and cardinality.
}
\description{
Evaluates common key variables (e.g., subject identifiers, visit times) across
multiple datasets in an R database list. Computes cross-table overlap, record
coverage, key cardinality (1:1, 1:many, many:many), and identifies orphan records.
}
\examples{
db <- list(
  patients = data.frame(id = 1:5, age = c(20, 30, 40, 50, 60)),
  vitals = data.frame(id = c(1, 1, 2, 2, 3, 6), bp = c(120, 122, 130, 132, 110, 115))
)
cbe_database_relationships(db)
}
