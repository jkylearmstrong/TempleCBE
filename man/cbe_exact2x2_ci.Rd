% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbe_exact2x2.R
\name{cbe_exact2x2_ci}
\alias{cbe_exact2x2_ci}
\title{Format Exact 2x2 Odds Ratio and Confidence Interval}
\usage{
cbe_exact2x2_ci(
  data,
  variable = NULL,
  by = NULL,
  digits = 1,
  conf.level = 0.95,
  midp = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame or a 2x2 table/matrix.}

\item{variable}{Character string column name (if \code{data} is a data frame).}

\item{by}{Character string grouping column name (if \code{data} is a data frame).}

\item{digits}{Integer number of decimal places for rounding (default 1).}

\item{conf.level}{Confidence level (default 0.95).}

\item{midp}{Logical or \code{NULL} (default). If \code{NULL}, automatically
defaults to \code{TRUE} if any cell count is zero.}

\item{...}{Additional arguments passed to \code{\link{cbe_exact2x2}}.}
}
\value{
A character string with the formatted odds ratio and confidence interval.
}
\description{
Computes the odds ratio and confidence interval from \code{\link{cbe_exact2x2}}
and returns a formatted character string (e.g. \code{"0.8 (0.3, 2.1)"}).
Useful for inline text reporting or table presentation.
}
\examples{
tab <- matrix(c(0, 10, 5, 15), nrow = 2)
cbe_exact2x2_ci(tab)
}
