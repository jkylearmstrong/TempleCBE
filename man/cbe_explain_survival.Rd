% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_survival.R
\name{cbe_explain_survival}
\alias{cbe_explain_survival}
\title{Explain Survival Models via survex and DALEX}
\usage{
cbe_explain_survival(
  model,
  data = NULL,
  y = NULL,
  predict_survival_function = NULL,
  predict_risk_function = NULL,
  times = NULL,
  label = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{model}{A fitted model object: \code{joint_model}, \code{coxnet_model},
\code{cv_coxnet}, \code{workflow}, or \code{model_fit}.}

\item{data}{A data frame containing predictor columns used to calculate explanations.
If \code{NULL} and \code{model} is a \code{joint_model}, the training predictors are used.}

\item{y}{A survival object or response variable (e.g. \code{Surv(time, status)}). If
\code{NULL} and \code{model} is a \code{joint_model}, the original survival object is used.}

\item{predict_survival_function}{Optional custom prediction function returning an
\eqn{N \times K} data frame or matrix of survival probabilities \eqn{S(t)}.}

\item{predict_risk_function}{Optional custom prediction function returning a numeric
vector of risk scores or cumulative hazard.}

\item{times}{A numeric vector of evaluation time points. If \code{NULL}, defaults to
unique event times in \code{y}.}

\item{label}{Character string naming the model in plots and summaries.}

\item{verbose}{Logical; if \code{TRUE}, progress messages are shown. Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{survex::explain_survival()}.}
}
\value{
A \code{survex_explainer} object compatible with \code{survex::model_parts()},
  \code{survex::model_performance()}, and \code{survex::predict_parts()}.
}
\description{
Adapts Tidymodels survival models (\code{workflows}, \code{parsnip} / \code{censored},
\code{coxnet_model}, and \code{joint_model}) to create a \code{survex} survival explainer.
Standardizes survival predictions into \eqn{N \times K} survival curves, bridging the
gap identified in survex issue #98.
}
\examples{
\dontrun{
if (requireNamespace("survex", quietly = TRUE) && requireNamespace("survival", quietly = TRUE)) {
  data(lung, package = "survival")
  df <- na.omit(lung[, c("time", "status", "age", "sex")])
  fit <- joint_model(df, survival::Surv(time, status) ~ age + sex)
  expl <- cbe_explain_survival(fit)
  mp <- survex::model_parts(expl)
}
}
}
