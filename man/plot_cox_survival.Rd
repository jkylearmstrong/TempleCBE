% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cox.R
\name{plot_cox_survival}
\alias{plot_cox_survival}
\title{Model-Predicted Survival Curves Stratified by Predictor}
\usage{
plot_cox_survival(
  fit,
  data,
  feature = NULL,
  id_col = NULL,
  n_tiles = 4,
  label_endpoints = TRUE,
  overlay_km = FALSE,
  base_size = 12
)
}
\arguments{
\item{fit}{A fitted \code{survival::coxph} model or a \code{cbe_cox} object.}

\item{data}{Data frame used for fitting the model.}

\item{feature}{Character name of the predictor column.}

\item{id_col}{Character name of subject ID column (default: "arl_number" or row number).}

\item{n_tiles}{Number of quantile bins for continuous predictors (default: 4).}

\item{label_endpoints}{Logical; if TRUE, repels labels for stratum values at the final time point.}

\item{overlay_km}{Logical (default: \code{FALSE}). If \code{TRUE}, overlays observed
Kaplan-Meier step curves (dashed) for each stratum on top of the Cox-predicted curves
(solid), sharing color by stratum, with a linetype legend distinguishing
"Cox-predicted" from "KM observed". The event time/status used are those the model
was fit with (\code{fit$y}).}

\item{base_size}{Base font size (default: 12).}
}
\value{
A ggplot2 object.
}
\description{
Generates stratified predicted survival curves from a Cox model. For continuous predictors,
values are automatically binned into quantile strata (e.g. quartiles).
}
