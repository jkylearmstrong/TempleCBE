% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pairwise_categorical.R
\name{plot_categorical_associations}
\alias{plot_categorical_associations}
\title{Plot Categorical Association Matrix Using corrplot}
\usage{
plot_categorical_associations(
  data,
  cols = NULL,
  method = c("cramer_v", "p_value"),
  title = "Categorical Association Matrix",
  fill_colors = c("#FFFFFF", "#9D2235"),
  tl.cex = 0.8,
  number.cex = 0.75,
  show_coef = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{cols}{Optional character vector of column names to include. If \code{NULL},
all factor and character columns are selected.}

\item{method}{Association metric: \code{"cramer_v"} (Cramer's V correlation,
default) or \code{"p_value"} (\eqn{-\log_{10}(p)} from categorical tests).}

\item{title}{Plot title (defaults to \code{"Categorical Association Matrix"}).}

\item{fill_colors}{Character vector for the color gradient (defaults to Temple palette).}

\item{tl.cex}{Label character expansion (default 0.8).}

\item{number.cex}{Numeric value character expansion inside circles (default 0.75).}

\item{show_coef}{Logical (default \code{TRUE}); whether to print coefficients.}

\item{...}{Additional arguments passed to \code{\link[corrplot]{corrplot}}.}
}
\value{
Invisibly, the association matrix.
}
\description{
Computes pairwise associations (Cramer's V or -log10 p-values) across all
categorical/factor columns in a data frame and renders an association matrix plot
via \pkg{corrplot}. This complements \code{\link{correlation_plot}} for numeric
variables.
}
\examples{
df <- data.frame(
  A = factor(sample(c("Yes", "No"), 50, replace = TRUE)),
  B = factor(sample(c("High", "Low"), 50, replace = TRUE)),
  C = factor(sample(c("Group1", "Group2", "Group3"), 50, replace = TRUE))
)
plot_categorical_associations(df)
}
