% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_helpers.R
\name{censoring_km}
\alias{censoring_km}
\alias{predict.censoring_km}
\title{Kaplan-Meier Estimate of the Censoring Distribution}
\usage{
censoring_km(truth)

\method{predict}{censoring_km}(object, time, left = FALSE, trunc = 0, ...)
}
\arguments{
\item{truth}{A right-censored [survival::Surv()] object with one element
per subject, such as the `.truth` column of [surv_subject_truth()].}

\item{object}{A `censoring_km` object.}

\item{time}{Numeric vector of times.}

\item{left}{If `TRUE`, the left limit \eqn{G(t^-)}, i.e. censoring strictly
before `time`.}

\item{trunc}{Lower bound applied to \eqn{G}, so weights stay finite.}

\item{...}{Not used.}
}
\value{
An object of class `censoring_km`, with `time` and `surv`; use
  `predict(object, time)` for \eqn{G(t)}.
}
\description{
Fits the "reverse" Kaplan-Meier estimator \eqn{G(t) = P(C > t)}, treating
censoring as the event, for inverse-probability-of-censoring weights.
Estimate it on the data a model was trained on, and apply it to the data
being scored.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  cens <- censoring_km(survival::Surv(c(2, 4, 6, 8), c(0, 1, 0, 1)))
  predict(cens, c(1, 2, 5, 7))
}
}
\seealso{
[graf_weights()]
}
