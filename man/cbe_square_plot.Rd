% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pairwise_categorical.R
\name{cbe_square_plot}
\alias{cbe_square_plot}
\title{Square Plot: Standard 4-Quadrant Contingency Report}
\usage{
cbe_square_plot(
  data,
  var1 = NULL,
  var2 = NULL,
  label1 = NULL,
  label2 = NULL,
  test = c("auto", "exact", "chisq", "fisher"),
  correct = FALSE,
  engine = c("ggplot", "corrplot"),
  size_range = c(6, 22),
  fill_colors = c("#F4F4F4", "#9D2235"),
  show_counts = TRUE,
  title = NULL,
  caption = NULL,
  p_format = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing the categorical variables, or a 2-way table/matrix.}

\item{var1}{Character string of the row variable column name (ignored if \code{data} is a table).}

\item{var2}{Character string of the column variable column name (ignored if \code{data} is a table).}

\item{label1}{Optional display label for \code{var1} (defaults to \code{var1}).}

\item{label2}{Optional display label for \code{var2} (defaults to \code{var2}).}

\item{test}{Hypothesis test engine: \code{"auto"} (default CBE hierarchy),
\code{"exact"} (force Central Fisher exact test for 2x2 or simulated Fisher for RxC),
\code{"chisq"} (force Pearson's Chi-squared test via \code{\link[stats]{chisq.test}}),
or \code{"fisher"} (force \code{\link[stats]{fisher.test}}).}

\item{correct}{Logical; whether to apply continuity correction when \code{test = "chisq"}
(default \code{FALSE} following CBE standard protocol).}

\item{engine}{Engine for balloon/square rendering: \code{"ggplot"} (default), \code{"corrplot"},
or \code{"ggpubr"}.}

\item{size_range}{Numeric vector of length 2 giving min and max glyph sizes for balloon and square plots
(default \code{c(6, 22)}).}

\item{fill_colors}{Character vector of colors for the palette or gradient. Defaults to
Temple University brand palette (\code{c("#F4F4F4", "#9D2235")}).}

\item{show_counts}{Logical (default \code{TRUE}); whether to print cell counts inside plots.}

\item{title}{Plot title. If \code{NULL}, defaults to \code{"{label1} \u00d7 {label2}"}.}

\item{caption}{Optional plot caption. If \code{NULL}, automatically computed and formatted
using \code{\link{pformat}} with test description.}

\item{p_format}{Optional p-value formatting function (default \code{\link{pformat}}).}

\item{...}{Additional arguments passed to underlying plotting functions.}
}
\value{
A \code{ggplot} object or invisibly the contingency table.
}
\description{
Dedicated wrapper for \code{\link{cbe_contingency_plot}} with \code{type = "square"}.
For 2x2 contingency tables, renders the standard 4-quadrant report:
\preformatted{
q1 | q2
q3 | q4
p = pformat
}
If any cell count is zero, hypothesis testing automatically defaults to the
mid-p version of Central Fisher's exact test via \code{\link{cbe_exact2x2}}.
}
