% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loco_mp_coxnet.R
\name{cbe_loco_mp_coxnet}
\alias{cbe_loco_mp_coxnet}
\title{Leave-One-Covariate-Out Inference with MiniPatch Ensembles (LOCO-MP) for Cox Models}
\usage{
cbe_loco_mp_coxnet(
  formula = NULL,
  data = NULL,
  x = NULL,
  y = NULL,
  B = 100,
  n_ratio = 0.7,
  m_ratio = 0.5,
  penalty = NULL,
  mixture = 1,
  eval_time = NULL,
  subject_id = NULL,
  alpha = 0.05,
  p_adjust = c("bonferroni", "BH", "fdr", "holm", "none"),
  trunc = 0.05,
  parallel = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{formula}{A formula with a \code{Surv()} outcome, e.g. \code{Surv(time, status) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in the model.}

\item{x}{An optional predictor matrix (used if \code{formula} and \code{data} are \code{NULL}).}

\item{y}{An optional \code{Surv} outcome object (used if \code{formula} and \code{data} are \code{NULL}).}

\item{B}{Number of minipatches to generate. Default is 100.}

\item{n_ratio}{Subsampling fraction for subjects/observations without replacement.
Default is 0.7.}

\item{m_ratio}{Subsampling fraction for predictors without replacement. Default is 0.5.}

\item{penalty}{Penalty parameter for \code{coxnet}. If \code{NULL}, defaults to 0 (unpenalized).}

\item{mixture}{Elastic-net mixing parameter (\eqn{\alpha \in [0, 1]}). Default is 1 (lasso).}

\item{eval_time}{Numeric vector of evaluation time points for IPCW Brier loss integration.
If \code{NULL}, defaults to deciles of event times.}

\item{subject_id}{Optional character column name identifying subjects for clustered /
counting-process start/stop data.}

\item{alpha}{Significance level for confidence intervals. Default is 0.05.}

\item{p_adjust}{Multiple testing correction method for p-values. Options include
\code{"bonferroni"}, \code{"BH"}, \code{"fdr"}, \code{"holm"}, or \code{"none"}.
Default is \code{"bonferroni"}.}

\item{trunc}{Lower truncation threshold for Kaplan-Meier censoring weights. Default is 0.05.}

\item{parallel}{Logical; if \code{TRUE} and \code{future} is available, runs patches in parallel.}

\item{seed}{Optional random seed for reproducible subsampling.}

\item{...}{Additional arguments passed to \code{coxnet()}.}
}
\value{
An S3 object of class \code{"cbe_loco_mp_coxnet"} containing:
  \item{results}{A tibble with columns \code{term}, \code{importance}, \code{std_error},
    \code{statistic}, \code{p_value}, \code{p_adjusted}, \code{conf_low}, \code{conf_high}.}
  \item{eval_time}{Evaluation time grid used for IPCW loss integration.}
  \item{B}{Number of successfully fitted minipatches.}
  \item{n_ratio, m_ratio}{Subsampling ratios used.}
  \item{alpha}{Significance level.}
  \item{p_adjust}{Multiple testing adjustment method.}
}
\description{
Implements the LOCO-MP statistical inference framework (Gan, Zheng, & Allen 2022)
for penalized Cox proportional hazards regression. Combines random minipatch
ensembling (subsampling both observations/subjects and covariates) with
out-of-bag Leave-One-Covariate-Out evaluation to produce distribution-free
feature importance estimates, standard errors, asymptotic z-tests, and
confidence intervals for survival models.
}
\examples{
\donttest{
if (requireNamespace("glmnet", quietly = TRUE) && requireNamespace("survival", quietly = TRUE)) {
  set.seed(42)
  n <- 60
  df <- data.frame(
    time = stats::rexp(n, 0.1) + 0.1,
    status = stats::rbinom(n, 1, 0.6),
    x1 = stats::rnorm(n),
    x2 = stats::rnorm(n),
    x3 = stats::rnorm(n)
  )
  fit <- cbe_loco_mp_coxnet(survival::Surv(time, status) ~ x1 + x2 + x3, data = df, B = 20)
  fit
  generics::tidy(fit)
}
}
}
\seealso{
[nested_cv_coxnet()], [cv_coxnet()], [coxnet()]
}
