% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coxnet.R
\name{predict.coxnet_model}
\alias{predict.coxnet_model}
\title{Predict From a `coxnet` Model}
\usage{
\method{predict}{coxnet_model}(
  object,
  new_data,
  type = c("linear_pred", "survival", "time"),
  penalty = NULL,
  eval_time = NULL,
  increasing = TRUE,
  ...
)
}
\arguments{
\item{object}{A [coxnet()] model.}

\item{new_data}{A data frame (or matrix) of new predictors.}

\item{type}{`"linear_pred"` for the linear predictor, `"survival"` for
survival probabilities at `eval_time`, or `"time"` for the restricted mean
survival time: the area under the predicted survival curve from 0 to the
last time in the training data.}

\item{penalty}{The penalty to predict at; defaults to the model's `penalty`.}

\item{eval_time}{For `type = "survival"`, the times to predict survival at.}

\item{increasing}{For `type = "linear_pred"`: if `TRUE` (the default, as in
tidymodels' \pkg{censored} package), the sign is flipped so larger values
mean longer survival. Use `FALSE` for glmnet's own sign, where larger values
mean higher risk.}

\item{...}{Not used.}
}
\value{
A tibble with one row per row of `new_data`: `.pred_linear_pred`,
  `.pred_time`, or `.pred`, a list-column of tibbles with `.eval_time` and
  `.pred_survival`.

Survival is \eqn{S(t \mid x) = \exp(-H_0(t) e^{x^\top \beta})}, with
\eqn{H_0} the Breslow estimate of the cumulative baseline hazard from the
training data at `penalty`. Each row's covariates are taken as constant from
time 0; to predict along a subject's start/stop covariate path, and score it,
use [cv_coxnet()].
}
\description{
Predict From a `coxnet` Model
}
