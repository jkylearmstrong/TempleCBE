% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_sas_script.R
\name{run_sas_script}
\alias{run_sas_script}
\title{Run a SAS Program in Batch Mode}
\usage{
run_sas_script(
  path,
  sas_path = find_sas(),
  log_dir = file.path(dirname(path), "logs"),
  list_dir = file.path(dirname(path), "list")
)
}
\arguments{
\item{path}{Path to the `.sas` program.}

\item{sas_path}{Path to the SAS executable. Defaults to [find_sas()].}

\item{log_dir}{Folder for the `.log` file. Defaults to `logs/` beside
`path`.}

\item{list_dir}{Folder for the `.lst` listing. Defaults to `list/` beside
`path`.}
}
\value{
The exit status of the SAS process (0 on success, 1 for warnings, 2
  for errors).
}
\description{
Runs a `.sas` file with the SAS executable, writing its log and listing to
separate folders next to the program (created if needed).
}
\examples{
\dontrun{
programs <- list.files("validation", pattern = "\\\\.sas$", full.names = TRUE)
vapply(programs, run_sas_script, integer(1))
}
}
\seealso{
[find_sas()], [cbe_sas_macro_path()]
}
