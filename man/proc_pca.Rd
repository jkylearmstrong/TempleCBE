% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proc_pca.R
\name{proc_pca}
\alias{proc_pca}
\title{Process and Plot Principal Component Analysis (PCA)}
\usage{
proc_pca(data, center = TRUE, scale = TRUE, ...)
}
\arguments{
\item{data}{A `prcomp` object, or a numeric matrix/data frame to fit PCA on.}

\item{center, scale}{Passed to \code{\link[stats]{prcomp}} (as `center` and
`scale.`) when `data` is raw data; ignored when `data` is already a
`prcomp` object.}

\item{...}{Further arguments passed to \code{\link[stats]{prcomp}} when
`data` is raw data.}
}
\value{
A tibble with one row per component: `component`, `eigenvalue`,
  `variance_pct`, `cum_variance_pct`.
}
\description{
Extracts a per-component variance summary from a PCA fit. Accepts either an
existing \code{\link[stats]{prcomp}} object or raw data, in which case
\code{\link[stats]{prcomp}} is run first.
}
\examples{
proc_pca(prcomp(mtcars[, 1:4], scale. = TRUE))

# Or fit the PCA in one step
proc_pca(mtcars[, 1:4], scale = TRUE)
}
