% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pi_anonymizer.R
\name{generate_pi_names}
\alias{generate_pi_names}
\title{Generate Random PI Names or Tokens}
\usage{
generate_pi_names(
  n = 1,
  format = c("synthetic", "token"),
  prefix = "PI_",
  n_chars = 8,
  rename_fn = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Integer, number of names to generate (default 1).}

\item{format}{Character, either `"synthetic"` (realistic surnames) or `"token"`
(cryptographic hash tokens).}

\item{prefix}{Character, prefix when format is `"token"` (default `"PI_"`).}

\item{n_chars}{Integer, number of hex characters from the hash digest when `format = "token"` (default 8).}

\item{rename_fn}{Optional function to rename or format generated tokens/names.}

\item{seed}{Optional integer, random seed for reproducibility.}
}
\value{
A character vector of length `n` (or a single character string if `n = 1`).
}
\description{
Generates random synthetic principal investigator (PI) names or cryptographic
pseudonym tokens (`PI_...`).
}
\examples{
generate_pi_names(1)
generate_pi_names(3, format = "token")
generate_pi_names(3, format = "token", n_chars = 4)
}
