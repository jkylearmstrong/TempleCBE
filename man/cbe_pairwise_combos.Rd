% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pairwise_categorical.R
\name{cbe_pairwise_combos}
\alias{cbe_pairwise_combos}
\title{Generate All Pairwise Categorical Combinations}
\usage{
cbe_pairwise_combos(data, cols = NULL, labels_df = NULL)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{cols}{Optional character vector of column names to include. If \code{NULL},
all factor and character columns are included.}

\item{labels_df}{Optional lookup data frame with columns \code{variable} and \code{label}.}
}
\value{
A tibble with columns \code{id}, \code{var1}, \code{var2}, \code{label1}, \code{label2},
  and \code{comparison_label}.
}
\description{
Helper for report generation to enumerate all pairwise combinations of
categorical variables in a dataset with sequential integer IDs and labels.
}
\examples{
df <- data.frame(
  A = factor(c("X", "Y")),
  B = factor(c("1", "2")),
  C = factor(c("M", "N"))
)
cbe_pairwise_combos(df)
}
