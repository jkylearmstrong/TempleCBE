% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbe_variable_roles.R
\name{cbe_variable_roles}
\alias{cbe_variable_roles}
\alias{cbe_set_roles}
\alias{cbe_get_roles}
\alias{cbe_get_predictors}
\alias{cbe_get_outcomes}
\alias{cbe_get_id_cols}
\alias{cbe_get_time_cols}
\title{Define and Manage Clinical Variable Roles}
\usage{
cbe_variable_roles(
  data,
  id = NULL,
  outcome = NULL,
  time = NULL,
  predictors = NULL,
  strata = NULL,
  weight = NULL,
  ignore = NULL,
  roles = NULL
)

cbe_set_roles(data, ...)

cbe_get_roles(data)

cbe_get_predictors(roles)

cbe_get_outcomes(roles)

cbe_get_id_cols(roles)

cbe_get_time_cols(roles)
}
\arguments{
\item{data}{A data frame or a named list of data frames (an R database).}

\item{id}{Character vector of column names acting as subject/patient/cluster identifiers.}

\item{outcome}{Character vector of column names acting as response/outcomes (\code{Y_var}).}

\item{time}{Character vector of column names acting as longitudinal visit/time variables (\code{Time_var}).}

\item{predictors}{Character vector of column names acting as features/predictors (\code{X_var}).}

\item{strata}{Character vector of column names used for stratification or subgroup analysis.}

\item{weight}{Character vector of column names used for case weights or survey weights.}

\item{ignore}{Character vector of column names to ignore/exclude from modeling.}

\item{roles}{Role table (e.g. from \code{\link{cbe_variable_roles}}) or data object.}
}
\value{
A tibble with columns \code{columns}, \code{role}, \code{X_var}, \code{Y_var},
  \code{ID_var}, \code{Time_var}, and \code{dataset_name} (when multi-table).
}
\description{
Assigns functional roles (e.g. predictor, outcome, identifier, time variable,
strata, weights) to variables across a single data frame or an entire R database list.
Produces standardized schemas compatible with \code{\link{validate_column_mapping}},
\pkg{recipes}, and modeling workflows.
}
\examples{
df <- data.frame(id = 1:5, time = 1:5, sbp = c(120, 130, 115, 140, 125), death = c(0, 0, 1, 0, 1))
roles <- cbe_variable_roles(df, id = "id", time = "time", outcome = "death", predictors = "sbp")
}
