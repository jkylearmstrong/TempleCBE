% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zip_render.R
\name{zip_render}
\alias{zip_render}
\title{Render a Quarto Document and Zip It With Its Dependencies}
\usage{
zip_render(
  input,
  formats = c("html", "pdf", "docx"),
  resources = NULL,
  detect = c("heuristic", "none"),
  build_dir = NULL,
  zip_name = NULL,
  copy_back_dir = NULL,
  include_sources = TRUE,
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{input}{Path to the input \code{.qmd} file.}

\item{formats}{Character vector of output formats (e.g. \code{c("html","pdf","docx")} or \code{"all"}).}

\item{resources}{Optional character vector of extra files to include, absolute or project-relative.}

\item{detect}{\code{"heuristic"} (default; scans the \code{.qmd} for likely file paths) or \code{"none"}.}

\item{build_dir}{Staging directory; defaults to a fresh temp directory.}

\item{zip_name}{Name of the resulting zip; defaults to \verb{<input-stem>.zip}.}

\item{copy_back_dir}{Where to copy the finished zip; defaults to \code{dirname(input)}.}

\item{include_sources}{Logical (default \code{TRUE}); include the \code{.qmd} and sidecar bib/tex/css files.}

\item{overwrite}{Logical (default \code{TRUE}); overwrite an existing zip at the destination.}

\item{verbose}{Logical (default \code{TRUE}); print progress messages.}
}
\value{
Invisibly, a list with the build directory, detected/copied resources, render outputs, and final zip path.
}
\description{
Renders \code{input} in an isolated build directory, copies in any
resources it references (explicitly, or heuristically detected from
quoted paths / \code{here::here()} calls), and zips the outputs together
with the source and sidecar files.
}
\examples{
\dontrun{
zip_render("report.qmd", formats = c("html", "pdf"))
}
}
