% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_model.R
\name{cv_joint_model}
\alias{cv_joint_model}
\title{Cross-Validation for Joint Models}
\usage{
cv_joint_model(
  data,
  outcome = survival::Surv(time, status) ~ .,
  v = 5,
  resamples = NULL,
  subject_id = NULL,
  engine = c("glmnet", "baguette", "stacks"),
  calibration = TRUE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{Survival formula with a \code{Surv()} outcome.}

\item{v}{Number of cross-validation folds (default 5).}

\item{resamples}{Optional pre-constructed \pkg{rsample} object.}

\item{subject_id}{Optional subject identifier column for grouped resampling.}

\item{engine}{Modeling engine: \code{"glmnet"}, \code{"baguette"}, or \code{"stacks"}.}

\item{calibration}{Logical; whether to calibrate status predictions (default \code{TRUE}).}

\item{parallel}{Logical; whether to run folds in parallel via \pkg{furrr}.}

\item{...}{Additional arguments passed to \code{\link[TempleCBE]{joint_model}}.}
}
\value{
An S3 object of class \code{c("cv_joint_model", "tbl_df")} summarizing
  comparative metrics across folds.
}
\description{
Evaluates the \code{\link[TempleCBE]{joint_model}} trio across cross-validation splits
(e.g. via \code{\link[rsample]{vfold_cv}} or \code{\link[rsample]{group_vfold_cv}}),
benchmarking survival, classification, and regression paradigms using the IPCW
Integrated Brier Score (\code{brier_survival_integrated}), Concordance, and Calibration.
}
\details{
For counting-process (start/stop) outcomes, \code{\link{predict.joint_model}} returns
one row per input row (one per interval); scoring needs one row per subject, so each
fold's predictions and truth are collapsed with \code{\link{surv_subject_truth}} (a
subject's last interval and final status) before computing metrics, mirroring
\code{\link[TempleCBE]{cv_coxnet}}'s handling of the same outcome type.
}
