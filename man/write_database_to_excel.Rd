% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_workbook.R
\name{write_database_to_excel}
\alias{write_database_to_excel}
\alias{write_workbook}
\alias{cbe_write_database}
\title{Write an R Database or Data Frames to an Excel Workbook}
\usage{
write_database_to_excel(
  x,
  path,
  include_metadata = TRUE,
  metadata = NULL,
  metadata_sheet = "METADATA",
  ...
)

write_workbook(
  x,
  path,
  include_metadata = TRUE,
  metadata = NULL,
  metadata_sheet = "METADATA",
  ...
)

cbe_write_database(
  x,
  path,
  include_metadata = TRUE,
  metadata = NULL,
  metadata_sheet = "METADATA",
  ...
)
}
\arguments{
\item{x}{A named list of data frames (an R database) or a single data frame/tibble.}

\item{path}{File path to save the \code{.xlsx} workbook.}

\item{include_metadata}{Logical; if \code{TRUE} (default), computes and appends
a data dictionary sheet generated via \code{\link{get_dataset_info}}.}

\item{metadata}{Optional pre-computed metadata data frame. If \code{NULL} and
\code{include_metadata = TRUE}, metadata is computed automatically.}

\item{metadata_sheet}{Character string naming the metadata worksheet (default: \code{"METADATA"}).}

\item{...}{Additional arguments passed to \code{\link[writexl]{write_xlsx}}.}
}
\value{
The file path invisibly.
}
\description{
Writes a named list of data frames (an R database) or a single data frame to
a multi-worksheet Excel workbook, with an optional automated data dictionary
worksheet.
}
\examples{
\dontrun{
db <- list(patients = data.frame(id = 1:3), labs = data.frame(id = 1:3, val = 4:6))
write_database_to_excel(db, "clinical_database.xlsx")
}
}
