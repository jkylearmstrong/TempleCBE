# Check if a FileOutputs object controls datasets (outputs an rds, xlsx, etc. file)

Check if a FileOutputs object controls datasets (outputs an rds, xlsx,
etc. file)

## Usage

``` r
controls_datasets(obj)
```

## Arguments

- obj:

  A FileOutputs object.

## Value

Logical.
