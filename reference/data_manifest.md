# Data Snapshot Manifest and Verification Utilities

Tools to track, snapshot, and verify analytical dataset copies against
upstream pipeline sources using cryptographic MD5 checksums and
in-memory object identity. Ensures downstream reports and presentation
decks never execute on stale or desynchronized data.
