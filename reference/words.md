# Combine Character Vector into Natural-Language Words

Combine Character Vector into Natural-Language Words

## Usage

``` r
words(x, none = "none")
```

## Arguments

- x:

  Character vector

- none:

  Fallback text when vector is empty (default: "none")

## Value

Character string (e.g., "A, B, and C")
